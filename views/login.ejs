<!DOCTYPE html>
<html lang="es">
<%- include('partials/head')%>

  <body>
    <%- include('partials/header')%>
    <div class="login">
      <form class="sesion" action="" method="POST">
        <h2 class="loginh2">Iniciar</h2>
        <input type="email" placeholder="Email" name="email" id="login-credentials">
        <input type="password" placeholder="Contraseña" name="pass" id="login-credentials">
        <% if (typeof errores != 'undefined') { %>
          <ul>
            <% errores.forEach(error =>{%>
              <li><%= error.msg %> </li>
            <% }) %> 
          </ul>
        <% } %>
        <label class="checkbox">
          <input type="checkbox" value="recordar" name="recordar" id="recordar">Remember me
        </label>
        <button class="btn login-button hvr-box-shadow-outset" type="submit">Entrar</button>        
        <a href="/usuario/registro" id="help-login">¿Todavia no tienes una cuenta?</a>
        <a href="" id="help-login">¿Olvidaste tu Contraseña?</a>
      </form>
    </div> 
        
      <%- include('partials/footer') %>

  </body>
</html>
<!DOCTYPE html>
<html lang="es">
<%- include('partials/head')%>
  <body>
    <%- include('partials/header')%>
      <main>
        <div class="container products-wrapper">
          <div class="row">
            <div class="col-12">
              <h2 class="products-title">Resultado de busqueda</h2>
            </div>
            <% dataBicis.forEach(producto => { %>
              <div class="col-12 col-md-4">
                <section class="product-box hvr-bounce-in">
                  <a href="/productos/detalle/<%- producto.id %>/<%- producto.section %>">
                    <figure class="product-box_image">
                      <img src="/images/imgProduct/<%- producto.image %> " alt="bicicleta" class="img-fluid">
                    </figure>
                    <article class="product-box_data">
                      <h6><%- producto.name %> </h6>
                      <h6><%- producto.marca %> </h6>
                      <span>$<%- (producto.price - producto.price * producto.discount / 100).toFixed(0).toString().replace(/\B(?=(\d{3})+(?!\d))/g,".") %></span>
                      <% if (producto.discount != 0) { %>
                        <span><%- producto.discount %>% OFF </span>
                      <% } %>
                      <i class="fas fa-truck"></i>
                    </article>
                  </a>
                </section>
              </div>
            <% }) %>  
          </div>
        </div>
      </main>
      <%- include('partials/footer') %>
  </body>
  </html>